{
  "name": "bun-app",
  "module": "./app/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run ./app/index.ts",
    "dev": "bun --watch run ./app/index.ts",
    "build": "bun build ./app/index.ts --outdir ./out --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/sharp": "^0.32.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "sharp": "^0.33.2"
  },
  "trustedDependencies": [
    "sharp"
  ]
}
{
  "name": "bun-app",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch run index.ts",
    "build": "bun build ./index.ts --outdir ./out --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/sharp": "^0.32.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "sharp": "^0.33.2"
  },
  "trustedDependencies": [
    "sharp"
  ]
}
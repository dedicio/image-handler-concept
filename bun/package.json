{
  "name": "bun-app",
  "module": "index.js",
  "type": "module",
  "scripts": {
    "start": "bun run index.js",
    "dev": "bun --env-file=../.env --watch run index.js",
    "build": "bun build ./index.js --outdir ./out --minify"
  },

  "dependencies": {
    "@aws-sdk/client-s3": "^3.515.0",
    "@aws-sdk/client-sns": "^3.516.0",
    "sharp": "^0.33.2",
    "shorthash2": "^1.0.3"
  },
  "trustedDependencies": [
    "sharp"
  ]
}